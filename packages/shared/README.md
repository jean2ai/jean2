# Jean2 Shared

Shared types and utilities used across server and client packages. Contains TypeScript interfaces, WebSocket message types, and common utilities.

## Installation

This package is included as a workspace dependency. Install via:

```bash
bun install
```

## Usage

Import types and utilities:

```typescript
import type { Session, Message } from '@jean2/shared';
```

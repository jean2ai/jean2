# Jean2 LSP Service

Language Server Protocol service providing code intelligence capabilities. Built with Bun and can be run as a standalone binary.

## Installation

Download the binary from GitHub releases for your platform.

## Usage

Run the LSP server:

```bash
./jean2-lsp
```

Connect via LSP protocol on the configured port (default: 3030).

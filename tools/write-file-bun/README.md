# write-file

Server tool for writing content to a file, creating it if it doesn't exist or overwriting if it does. Reports whether the file was newly created or overwritten.

## Requirements

- **Runtime**: `bun`

## Parameters

- `path` (required): Path to the file
- `content` (required): The content to write to the file

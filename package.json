{
  "name": "jean2",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Jean2 - AI Agent Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:server & bun run dev:client & bun run dev:lsp",
    "dev:server": "cd packages/server && bun --watch --env-file=../../.env src/index.ts",
    "dev:client": "cd packages/client && bun run dev",
    "dev:lsp": "cd packages/lsp && bun --watch --env-file=../../.env src/index.ts",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/diff": "^8.0.0",
    "eslint": "^10.0.2",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "diff": "^8.0.3"
  }
}

### Changed

- **`sessionId` on error events** — all server-to-client error message types now carry an optional `sessionId` field. This lets the client correlate errors to specific pending operations (fork, revert, compact, etc.) and show contextual toast notifications instead of generic error messages. The `'error'` event emission in `routeServerMessage` now passes `sessionId` as a third argument.
